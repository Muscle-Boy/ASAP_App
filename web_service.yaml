apiVersion: v1
kind: Service
metadata:
  name: ASAP-service
spec:
  selector:
    app: ASAP
  ports:
    - protocol: TCP
      port: 80      # Use port 80 to connect with INGRESS!
      targetPort: 8085